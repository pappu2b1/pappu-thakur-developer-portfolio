{
  "name": "pappu-thakur-portfolio",
  "private": true,
  "scripts": { "dev": "npm run dev --prefix frontend", "build": "npm run build --prefix frontend", "server": "npm run dev --prefix backend" },
  "workspaces": ["frontend", "backend"]
}
